==,111
main system {
  [in=null system < system {[ in _ < system {break}]}]
  [out=null system < in {[ out _ < system {break}]}]
  [ out < self {break}]
  [ out < self {break}]
  [ out < self {break}]
  break
}
